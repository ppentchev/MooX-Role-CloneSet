#!/usr/bin/env execlineb
# SPDX-FileCopyrightText: Peter Pentchev <roam@ringlet.net>
# SPDX-License-Identifier: Artistic-1.0-Perl

if {
	minil test
}
if {
	emptyenv -P
	tools/el-podman-run-unit-tests
} if {
	emptyenv -P
	tools/el-format check
} echo "Seems fine"
